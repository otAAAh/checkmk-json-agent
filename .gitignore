# Editor / swap files
.*.swp
*~
*kate-swp
*.iml

# OS
.DS_Store
.directory

# IDEs
.idea/
.vscode/*
*.code-workspace
**/launch.json
.history/
.ionide

# Python
.cache
*.pyc
__pycache__/
.coverage
.coverage.*
htmlcov/
.hypothesis/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.ropeproject
.python-version

# Virtualenv
/.venv/
.venv.lock

# Build artifacts (MKP package output)
*.mkp

# Frontend build output (built by `make frontend` from frontend/; the Vue app
# under frontend/src is the source of truth). Shipped in the mkp, not committed.
web/htdocs/json_api/wizard/
frontend/node_modules/
frontend/dist/

# Generated translation template (regenerate with `make pot`); the .po files
# under locales/*/LC_MESSAGES/ are the committed source of truth.
locales/json_api.pot

# AI agent local config
CLAUDE.md
/.claude/settings.local.json
